# Fast deployment checklist

## GitHub

```bash
git init
git add .
git commit -m "Launch MemoryGPT interactive demo"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/memorygpt-kvmemnet.git
git push -u origin main
```

## Streamlit Community Cloud

- Repository: `YOUR_USERNAME/memorygpt-kvmemnet`
- Branch: `main`
- Main file: `app.py`

## Portfolio links

Add the deployed URL to:

- GitHub repository **About → Website**
- LinkedIn **Featured** section
- Your resume project entry
